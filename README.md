# Bajaj_FullStack
Full-stack application that processes directed graph edges to construct hierarchical tree structures, detect cycles, handle invalid and duplicate inputs, and visualize results using a React-based interface.
